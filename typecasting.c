#include<stdio.h>
#include<conio.h>
void main()
{
    int n =4,m=5;
    float add;
    add=n+m;
    printf(" addition of two numbers :%.2f",add);
    //In the type casting we convert the data types just like int to float
}